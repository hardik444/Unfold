MONGODB_URI=__REPLACE_WITH_MONGODB_URI__
ADMIN_EMAIL=__REPLACE_WITH_ADMIN_EMAIL__
ADMIN_PASSWORD=__REPLACE_WITH_ADMIN_PASSWORD__
JWT_SECRET=__REPLACE_WITH_JWT_SECRET__

# Copy this file to `.env.local` and fill in real values locally. Do NOT commit
# `.env.local` with secrets; it's ignored by the repository.